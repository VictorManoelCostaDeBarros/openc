{
  "scripts": {
    "test": "npx hardhat coverage",
    "deploy": "npx hardhat run scripts/deploy.ts --network sepolia",
    "ignition": "npx hardhat ignition deploy ignition/modules/OpenC.ts --network sepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.5.0",
    "hardhat": "^2.24.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
